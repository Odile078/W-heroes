{{#partial "content"}}

    <h1> List o' stuff to do!</h1>

    {{#if heroes}}
        {{#each heroes }}
            <p>Current hero:</p>
            <p>{{ name }} <a href="/heroes/{{id}}">read more...</a></p>
        {{/each}}
    {{else}}
        <p>No heroes!</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}